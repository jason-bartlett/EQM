/*
var totalPages = workingCopy.model().allPages().filter(page => {
     return getModule(page).name === getModule(domainModel).name;
 });
 totalNumberPages += totalPages.length;
 console.log(`Total Pages: ${totalPages.length}`);
 
 
 console.log(`Total Stats`);

        console.log(`Total Application Objects: ${totalNumberPages+totalNumberEntities+totalNumberMicroflows}`);
         
        console.log(`Total Pages: ${totalNumberPages}`);
   
        console.log(`Total Microflows: ${totalNumberMicroflows}`);
     
        console.log(`Total Entities: ${totalNumberEntities}`);
 
 
 
 
 */
